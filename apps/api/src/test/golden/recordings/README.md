# Approved recordings

Place approved golden recordings here (16 kHz, mono, 16-bit PCM WAV) and list them in
`../manifest.json` with `"source": "approved-recording"`. This directory is git-ignored:
recordings are real people's voices and are distributed out of band, never committed.
